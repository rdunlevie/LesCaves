<%= form_for(vineyard) do |f| %>
  <% if vineyard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vineyard.errors.count, "error") %> prohibited this vineyard from being saved:</h2>

      <ul>
      <% vineyard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vineyard_id %>
    <%= f.number_field :vineyard_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :has_sub_vineyard %>
    <%= f.check_box :has_sub_vineyard %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
